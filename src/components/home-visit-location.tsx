import { StoreLocationSection } from "@/components/store-location-section";

/** Homepage — makes the Accra Mall store impossible to miss. */
export function HomeVisitLocation() {
  return <StoreLocationSection id="visit-accra-mall" featured />;
}
